"use client";
import { FormEvent, useState } from "react";
import { call,Detail,Input,Notice,Packet,pretty,s,State,usePilot } from "./PilotArtifactShared";

export default function PilotPermissions({pilotId}:{pilotId:string}){const{data,error,reload}=usePilot(pilotId);if(error)return <State text={pretty(error)}/>;if(!data)return <State text="Loading evidence permissions…"/>;return <Loaded data={data} pilotId={pilotId} reload={reload}/>;}
function Loaded({data,pilotId,reload}:{data:Detail;pilotId:string;reload:()=>Promise<void>}){const p=data.permissions;const[busy,setBusy]=useState(false),[notice,setNotice]=useState(""),[testimonial,setTestimonial]=useState(s(p,"testimonial_status")||"not_requested"),[caseStudy,setCaseStudy]=useState(s(p,"case_study_status")||"not_requested"),[reference,setReference]=useState(s(p,"reference_status")||"not_requested"),[funder,setFunder]=useState(s(p,"funder_evidence_status")||"not_requested"),[ref,setRef]=useState(s(p,"permission_reference")),[notes,setNotes]=useState(s(p,"notes"));async function save(e:FormEvent){e.preventDefault();setBusy(true);try{await call("/api/institutions/ops/pilots/action",{method:"POST",body:JSON.stringify({action:"save_permissions",pilotId,testimonialStatus:testimonial,caseStudyStatus:caseStudy,referenceStatus:reference,funderEvidenceStatus:funder,permissionReference:ref,notes})});setNotice("Permission statuses saved with source reference.");await reload();}catch(e){setNotice(e instanceof Error?pretty(e.message):"Save failed");}finally{setBusy(false);}}return <Packet title="Evidence & Publication Permissions" subtitle={s(data.institution,"name")}><Notice>“Granted” is an administrative record only when an actual permission/approval reference exists. The system does not infer consent from participation, positive feedback, or pilot completion.</Notice>{notice&&<div className="rounded-xl border border-[#76ead6]/20 p-4 text-sm">{notice}</div>}<form onSubmit={save} className="grid gap-4 md:grid-cols-2">{[["Testimonial",testimonial,setTestimonial,false],["Case study",caseStudy,setCaseStudy,false],["Institutional reference",reference,setReference,false],["Grant/funder evidence",funder,setFunder,true]].map(([label,value,setter,allowNA])=><label key={label as string} className="block"><span className="mb-2 block text-xs font-black uppercase tracking-[.12em] text-slate-400">{label as string}</span><select className="input w-full" value={value as string} onChange={e=>(setter as (v:string)=>void)(e.target.value)}>{["not_requested","requested","granted","declined","withdrawn",...(allowNA?["not_applicable"]:[])].map(v=><option key={v} value={v}>{pretty(v)}</option>)}</select></label>)}<div className="md:col-span-2"><Input label="Permission / approval reference" value={ref} set={setRef}/></div><label className="md:col-span-2"><span className="mb-2 block text-xs font-black uppercase tracking-[.12em] text-slate-400">Permission notes</span><textarea className="input min-h-28 w-full" value={notes} onChange={e=>setNotes(e.target.value)}/></label><div className="md:col-span-2"><button disabled={busy} className="button-primary">Save permission record</button></div></form></Packet>}
