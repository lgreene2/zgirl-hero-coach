export default function VersionBadge() {
  // Build/version metadata remains available through deployment and release records.
  // Do not expose internal release identifiers as a floating control in the public experience.
  return null;
}
